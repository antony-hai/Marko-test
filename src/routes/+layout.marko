<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="icon" type="image/png" sizes="32x32" href="./favicon.png">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="A basic Marko app.">
  <title>${$global.meta.pageTitle || 'Marko'}</title>
</head>
<body>
  <${input.renderBody}/>
</body>
</html>

style {
  html, body {
    font-family: system-ui;
    padding: 0;
    margin: 0;
    height: 100%;
    color: #fff;
  }

  div,p, h1, h2, h3, h4,h5,h6, ul, li, section, header, nav, section, article, span, a, img, input{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  code {
    color: #fc0;
  }
  a {
    color: #09c;
  }
}